import { ColorScheme } from 'app/Models/ColorScheme';

export const AppColors = {
    spectrumGreen: {
        beginColor: '#08ea72',
        endColor: '#2af598'
    } as ColorScheme,
    spectrumYellow: {
        beginColor: '#ffffff',
        endColor: '#ffffff'
    } as ColorScheme,
    spectrumOrange: {
        beginColor: '#fc7466',
        endColor: '#e48f70'
    } as ColorScheme,
    spectrumPink: {
        beginColor: '#e45379',
        endColor: '#d17e94'
    } as ColorScheme,
    spectrumPurple: {
        beginColor: '#ffffff',
        endColor: '#ffffff'
    } as ColorScheme,
    spectrumAnil: {
        beginColor: '#ffffff',
        endColor: '#ffffff'
    } as ColorScheme,
    spectrumBlue: {
        beginColor: '#ffffff',
        endColor: '#ffffff'
    } as ColorScheme,
    spectrumBabyBlue: {
        beginColor: '#68ead0',
        endColor: '#56b8e5'
    } as ColorScheme
};
